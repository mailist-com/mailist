<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="007-create-campaigns" author="marketing-automation">
        <comment>Create campaigns and related tables</comment>

        <createTable tableName="campaigns">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="tenant_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="subject" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="body" type="TEXT">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(50)" defaultValue="DRAFT">
                <constraints nullable="false"/>
            </column>
            <column name="scheduled_at" type="TIMESTAMP"/>
            <column name="sent_at" type="TIMESTAMP"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Add foreign key for tenant -->
        <addForeignKeyConstraint
            baseTableName="campaigns"
            baseColumnNames="tenant_id"
            referencedTableName="organizations"
            referencedColumnNames="id"
            constraintName="fk_campaigns_tenant"
            onDelete="CASCADE"/>

        <!-- Create campaign_recipients table -->
        <createTable tableName="campaign_recipients">
            <column name="campaign_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="recipient_email" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(50)" defaultValue="PENDING">
                <constraints nullable="false"/>
            </column>
            <column name="sent_at" type="TIMESTAMP"/>
            <column name="opened_at" type="TIMESTAMP"/>
            <column name="clicked_at" type="TIMESTAMP"/>
        </createTable>

        <!-- Add primary key -->
        <addPrimaryKey tableName="campaign_recipients" columnNames="campaign_id,recipient_email" constraintName="pk_campaign_recipients"/>

        <!-- Add foreign key -->
        <addForeignKeyConstraint
            baseTableName="campaign_recipients"
            baseColumnNames="campaign_id"
            referencedTableName="campaigns"
            referencedColumnNames="id"
            constraintName="fk_campaign_recipients_campaign"
            onDelete="CASCADE"/>

    </changeSet>

</databaseChangeLog>
